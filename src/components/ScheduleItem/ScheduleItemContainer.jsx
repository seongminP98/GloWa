import React from 'react';
import ScheduleItemPresenter from './ScheduleItemPresenter';

const ScheduleItemContainer = (props) => {
  return <ScheduleItemPresenter {...props} />;
};

export default ScheduleItemContainer;
